export const keywords = {
  PROGRAM: /(program)/,
  PROCEDURE: /(procedure)/,
  BEGIN: /(begin)/,
  END: /(end)/,
  IF: /(if)/,
  THEN: /(then)/,
  ELSE: /(else)/,
  WHILE: /(while)/,
  DO: /(do)/,
  READ: /(read)/,
  WRITE: /(write)/,
  VAR: /(var)/
}